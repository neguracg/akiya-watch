# BUGLOG

## 分析済み（2026-07-28 レビュー）

- 2026-07-26 | 状態同期(watch.py `_FILTER_JS`)がnull値の意味論不備で★/非表示/検索条件を消失させる複合バグ（exareas null化で全タブ0件表示に固定/空レコードのpullで★・非表示が全消去/pull失敗後もpushしサーバの正データを貧弱なローカルで上書き/デバウンス中の離脱で変更消失） | 原因: 「サーバに未設定のキー=null」をそのまま送受信・適用しており、「サーバに無いキー＝ローカルを維持」という意味論が実装されていなかった | commit: fc3d656 | #data-loss #state-sync @sekkei → 対策: なし（単発）
- 2026-07-26 | state-api: CORS_ORIGIN起動時検証が不十分で、カンマ区切り複数指定/スキーム欠落/大文字混在/既定ポート明記(:443)の4パターンが検証を素通りして起動し、その後ACAOヘッダを一切返さず同期が無言死する(敵対的レビューで実測) | 原因: 検証が`*`/末尾スラッシュ/埋め込み空白の3パターンの文字列チェックのみで、urllib.parse.urlsplitによる構造的検証が無かった | commit: fc3d656 | #config-validation #silent-failure @jissou → 対策: なし（次週: 設定値の起動時検証パターン候補・error-policy受け皿見込み）
- 2026-07-26 | state-api: DB_CONNECTION_LIMIT/PUT_RATE_LIMIT_MAX/GET_RATE_LIMIT_MAX/MAX_BODY_BYTESに起動時のレンジ検証が無く、DB_CONNECTION_LIMIT=0で全リクエスト恒久503(healthzは「db down」と偽装)・=-1でthreading.Semaphore生成がValueErrorとなりモジュールimport自体が失敗してrestart:unless-stoppedがクラッシュループ・=500で上限対策自体が無意味化(敵対的レビューで実測) | 原因: 環境変数をint変換するのみでレンジチェックが無く、さらにDB_CONNECTION_LIMITをモジュール読み込み時に直接threading.Semaphoreへ渡していたため異常値がフェイルファストなRuntimeErrorではなくimport時例外になっていた | commit: fc3d656 | #config-validation #crash-loop @jissou → 対策: なし（次週: 設定値の起動時検証パターン候補・error-policy受け皿見込み）
- 2026-07-26 | `--rebuild`(クロールなし再生成)の初版が本番レポートを劣化させて上書きした。賃貸の種別が全件「賃貸その他」・間取り0件・敷金0件・建築可否△が193→1件になった状態でreports/index.htmlを書き換え、公開しかけた（実測比較で発覚しgit checkoutで復元） | 原因: スナップショットが判定結果を保存しておらず再現できないのに、出力先を本番成果物と同じにしていた。「劣化しうる再生成」と「本番成果物」を分離する設計になっていなかった | commit: 7fd9235 | #data-quality #destructive-default @sekkei → 対策: なし（単発）
- 2026-08-31 | モバイル一覧をカード型(1物件=14行のdisplay:grid)にした直前コミットが実機で「めちゃ見づらい」と不評になり作り直しになった。1画面に1〜2件しか表示できず、6000件規模を「ざっと眺める」用途に使えなかった | 原因: カード化を採用した際、大量件数を走査する用途での1画面あたりの表示行数という要件を検討していなかった。**同じ失敗の2回目**（2026-06-17にもカード化→翌日「PC・iPhoneとも同じ表」で撤回済み＝ログ抽出で判明。1回目が文書に残っておらず再発した） | commit: 6c9e94c | #ui-density @sekkei ヨコテン:済0件(対象はwatch.py内の.cardtbl→.listtbl4テーブルのみで他に同型箇所なし)/恒久化:docs/DECISIONS.md §3 に「モバイルはカード表示にしない」を明記 グローバル:済(sekkei-policy references/ui-patterns.md の「横スクロール前提の固定幅テーブルは狭い画面で破綻する→カード型に切り替える」を無条件の型として読んだのが直接原因。件数規模による条件分けを同教訓へ追記した)
- 2026-09-05 | 文書に書いたコミットハッシュが `git pull --rebase` 後に存在しない値になった（WORKLOGの 7e5b27f→b9e53f3。8/31のBUGLOG 40f82d5→6c9e94c に続く2回目） | 原因: pushが夜間クロールのコミットに弾かれ rebase でローカルコミットが付け替わるのに、rebase前のハッシュを文書へ書いていた | commit: 775263e | #doc-drift @jissou ヨコテン:済2件(WORKLOG/BUGLOG)/恒久化:kankyo-policy NOTES「ハッシュは push 成功後に書く。Actionsが自動コミットするリポジトリでは必ず rebase が入る」 グローバル:済(自動コミットbotがいるリポジトリ全般で起きる。kankyo-policy NOTES.md に追記)
- 2026-09-05 | 日次Actions定期実行が「実行ウォールクロック上限1800s超過」で打ち切られ、巡回順の並べ替え次第で末尾サイトがその回だけ更新されない状態が発生していた（`gh run view --log`で直近5回を実測: 2026-08-31/09-01(2回)/09-02/09-03の5回中3回で発生・打ち切り件数はそれぞれ12/7/4件。exit 1=部分成功として正常終了扱いのため気づかれず、監視エリア拡大(101→118サイト化)の実測着手で発覚） | 原因: `RUN_WALLCLOCK_LIMIT`(1800秒=30分)をサイト数増加に合わせて見直しておらず、2026-07-12のバッチ4以降じわじわ実行時間が上限へ近づいていた | commit: af5a38e, 2653b30, 481fd14 | #capacity-limit @jissou ヨコテン:済2件(watch.pyのRUN_WALLCLOCK_LIMITと.github/workflows/daily.ymlのtimeout-minutesは同じ「全体処理時間の壁時計上限」機構の2箇所で、同一コミットで55分/70分へ揃えて引き上げ済み。他に同型の壁時計上限は無し)/恒久化:見送り(この機構自体は1本のため部品化の余地なし。次回また上限に近づいたら本行を参照して閾値を見直す) グローバル:済(固定の全体壁時計上限×周期実行で監視対象リストが伸び続ける設計は他プロジェクトでも起きる汎用パターンで、exit 0/1のため気づかれにくい。kankyo-policy unattended.mdへの追記候補として次回retro/bug-reviewに引き継ぐ)
