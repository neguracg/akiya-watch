# 指示書: 実装からのリバースで SPEC（要件＋設計）を起こす

発注: 2026-09-05 オーケストレータ（メインセッション）
目的: セッション分割運用に移るため、「決まっていること・大事なこと」を次のセッションが
**このファイルだけ読めば**分かる状態にする。腱さん（PM・非エンジニア寄り）が読む前提で、
理解の速さを最優先（BLUF・表・短文）。

## 出力（このファイル以外は作らない・触らない）
1. `docs/SPEC.md` — 新規。下記の章立て。目安 300〜500 行。**日付付きチェンジログは書かない**（WORKLOGが正本）
2. `docs/WORKLOG.md` — 新規。`git log --date=short --format="%ad %h %s"` を古い順に読み、
   **1日1行**（同日複数コミットは要約して1行）で「日付 | 何をした | commit」の追記型ログにする。
   先頭に「このファイルは追記のみ。時系列の正本」と1行書く

## 読んでよいもの（この順で。全部読む）
- `CLAUDE.md`（プロジェクト）・`docs/README.md`・`docs/requirements.md`（v0.3=2026-06-14の初期要件。
  **現状と食い違う箇所は現状（コード）を正**とし、初期要件は「経緯」として扱う）・`docs/HANDOFF.md`（封印済み）
- `urls.yaml` 全体（`filters:` ブロック、`sites:` の各エントリの属性、`sources_extra:`）
- `watch.py` 全体（5,089行。関数一覧: `grep -n "^def \|^class " watch.py`。CSS/JSは文字列定数内）
- `state-api/README.md`・`state-api/app/main.py`・`.github/workflows/daily.yml`・`SOURCES.md` 冒頭
- `C:\Users\mntns_05egufd\.claude\projects\C--Claude-30-akiya-watch\memory\*.md`（決定の経緯。
  古い記述はコードで裏取りしてから書く）
- `docs/BUGLOG.md`

## SPEC.md の章立て（この順・この見出し）
0. **正本宣言**（3行）: 仕様の正本=本書／URLマスタ=urls.yaml／時系列=docs/WORKLOG.md／バグ=docs/BUGLOG.md／残課題=消込表(72_DevSupport)
1. **目的と利用者**: 何のためのツールか（4タブそれぞれの目的）。利用者3名のキー（ken/yumiko/ayako）と配布URL形式
2. **用語**: タブ名・種別・適合/NG/注意/本命・検出日/消滅・除外エリア・状態同期 等
3. **監視対象（urls.yaml の構造）**: サイトエントリの属性（id/name/url/method/tab/filter_keywords/…）の意味を表で。
   タブ別のサイト数（実測）。`sources_extra` の意味（保留/フェーズ2の記録）。**URL変更の手順はCLAUDE.mdを参照**（重複記述禁止）
4. **判定ルール（filters）**: ベース／camp／rent の各値と、種別別上限・注意/本命キーワード・除外エリアの効き方。
   「適合」の定義を EARS 1文（「〜のとき、システムは〜すること」）で数個
5. **データの流れ**: クロール→抽出(parse_*)→レコード化(_make_record の全フィールド表)→差分(first_seen/last_seen/archive)→
   snapshot保存(20項目の判定結果も保存する理由)→レポート生成(埋め込みDATA)→Pages公開。図は簡単なテキスト図でよい
6. **レポート画面の仕様**: 4タブ・列定義(colsFor)・列フィルタ/並べ替え・★/非表示・除外エリア(タブ別)・新着/NGログ/消滅/サイト別サマリ・
   ヒートマップ(価格/面積/坪単価/検出日の閾値と色)・モバイル表示(700px以下で何が変わるか)
7. **状態同期（?u=キー）**: ハイブリッド構成の要点・localStorageキー一覧・API endpoint・「どちらが勝つか」の規則・キー切替時の挙動
8. **運用**: GitHub Actions 夜間実行（時刻・所要）・Pages公開URL・`--rebuild`（何ができて何ができないか）・SOURCES.md・
   手動でクロールを走らせる方法（`gh workflow run daily.yml`）
9. **アダプタの型**: `parse_*(first_html, base_url, filter_keywords, filters, session)` の契約・新サイト追加の手順（urls.yaml エントリ＋parse関数＋SOURCES）・
   既存アダプタ一覧（サイト→関数→方式(静的/ページャ/リンク監視)）を表で
10. **制約・非機能**: 外部ライブラリ方針、bot対策で落ちるサイトの扱い、public repo である前提（個人情報を入れない）、
    1回のクロール所要時間の目安
11. **未決・フェーズ2**: sources_extra の要約＋メモリに残る宿題（Uptime Kuma登録・実機2台同期確認 等）

## 書き方の規律
- **実物で確認したことだけ書く。** 推測は「（推測）」と付ける。行番号は書かない（すぐ腐る）。関数名は書いてよい
- 数値（閾値・色コード・件数）はコードから写す。件数は「2026-09 時点で約N」のように範囲で
- 初期要件 requirements.md の内容のうち**現状に残っているもの**だけ SPEC に取り込む。消えた/変わった項目は
  11章の末尾に「初期要件からの変更」として 5〜10 行で列挙（何が・どう変わったか）
- 同じことを2章に書かない。参照で済ます
- 完了時: 触ったファイル2つの絶対パスを報告に書く。git操作（add/commit）はしない（オーケストレータが行う）
