# akiya-watch 恒久ルール

## 現在地・入口
- 静岡県東部〜伊豆〜富士周辺の不動産監視ツール。4タブ（更地／家付き土地／キャンプ場土地／賃貸）。
  毎朝 GitHub Actions がクロールし GitHub Pages に公開。★/非表示は VPS の状態API（`?u=キー`）で同期。
- 文書の入口は `docs/README.md`（使い方と文書の地図）。仕様は `docs/SPEC.md` を読んでから触る。

## 正本マップ
| 事実 | 正本 | 同期ルール |
|---|---|---|
| 仕様（要件＋設計の現状） | `docs/SPEC.md` | 仕様を変える改修（B変更）は**同じコミット**で該当節を更新 |
| 決定の理由・設計原則・却下案 | `docs/DECISIONS.md` | 新しい決定は末尾に追記（日付・何を・なぜ・決めた人） |
| 監視対象URL・判定閾値 | `urls.yaml` | 下記「URL マスタ」の手順のみ |
| 時系列（何をしたか） | `docs/WORKLOG.md` | 追記のみ。作業単位ごとに1行 |
| バグ | `docs/BUGLOG.md` | グローバル CLAUDE.md の記録ルール |
| 残課題 | 消込表（`C:\Claude\72_DevSupport`） | 文書には要約と参照のみ |
| 監視ソースの稼働状況 | `SOURCES.md` | `watch.py` が自動生成。手で編集しない |

## 検証コマンド
- 表示だけの確認: `py watch.py --rebuild` → `reports/_preview.html`（クロールなし・公開レポートに触らない）
- 1サイト試走: `py watch.py --only=<id部分一致> --dry-run`
- 公開反映: `gh workflow run daily.yml`（約30分。pushだけでは公開ページは変わらない）
- 見た目を変えたら 1280px と 375px の両方で実測（モバイルはカード化しない。`docs/DECISIONS.md` §3）

## プロジェクト固有規約

### URL マスタの扱い
- `urls.yaml` が監視対象URLの**唯一のマスタ**である。
- URL は**変更しない**。記憶・推測・訓練データからURLを生成・修正することを禁止する。
- 404 / 405 等が返っても、URLを書き換えてはならない。「要確認」として報告するに留める。

### URL 変更の唯一の手順
URLの追加・修正・削除は、以下の **(a) と (b) の両方** を経た変更案を作成し、
**ユーザーの明示承認を得てからのみ** `urls.yaml` に適用する。
- (a) `Invoke-WebRequest`（pwsh）で対象URLのHTTPステータスコードを**実測**する。
- (b) `WebSearch` / `WebFetch` で現行の正しいURLを**確認**する。
(a) または (b) の一方だけ、あるいは承認なしでの適用は禁止。

### フィルタ定義
- 価格・面積の閾値や関心/注意キーワードは `urls.yaml` の `filters:` ブロックを唯一の定義元とする。
- これらの値を `watch.py` 等のコードに直接書かない（ハードコード禁止）。

### 生成物の信頼性
- `data/snapshots/` と `reports/` の内容は `watch.py` の実行結果であり、URLが正しいことが前提。
  URLの正しさを生成物から逆算して判断しない。
- `--rebuild` は `reports/_preview.html` **だけ**に書く。公開レポートを書き換える経路にしない。

### やらないこと
- ローカルのタスクスケジューラ登録はしない（定期実行は GitHub Actions が正）。
- リポジトリは public。個人情報・実メール・キーの実値をコミットしない。
